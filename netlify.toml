[build]
  base = "client"
  command = "cd ../types && yarn && cd ../client && yarn && yarn build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200